<%= form_for(@post, :remote => true) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :user_id, :value => current_user.id %>
<table bgcolor = "#FFFFFF">
  <div class="field">
     <br/>
     
      <tr>
        <td>
    <% unless current_user.profile.avatar_file.nil? %>
        <%= image_tag url_for(:controller => "profiles", :action => "code_image"),{ :class => "thumb_image_post"}  %>
     <% else %>
        <div id ="image">
        <%=  image_tag("missing.png",height: 70,width: 70) %>
      </div>
    <% end %>
        </td>
     <td><%= f.text_area :current_post,:class => "post_textarea",:rows => "2", :cols => "2" %></td>
   </tr>
  </div>
  <tr>
    <td></td>
    <td><div class="a" ">
    <%= f.submit "post", :style=> "width:100px", :class => "postbtn", :id => "save" %>
    </div></td>
  </tr>
</table>  

<% end %>

