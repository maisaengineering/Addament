<%= form_for(@my_activity, :remote => true) do |f| %>
  <% if @my_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_activity.errors.count, "error") %> prohibited this my_activity from being saved:</h2>

      <ul>
      <% @my_activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.label :current_activity %><br />
    <%= f.text_area :current_activity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<% @all_activites.each do |activity| %>

    <%=  activity.current_activity %>
<% end %>
