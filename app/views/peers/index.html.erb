<h1>Listing peers</h1>

<table>
  <tr>
    <th>User</th>
    <th>Req to peer</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @peers.each do |peer| %>
  <tr>
    <td><%= peer.user_id %></td>
    <td><%= peer.req_to_peer_id %></td>
    <td><%= link_to 'Show', peer %></td>
    <td><%= link_to 'Edit', edit_peer_path(peer) %></td>
    <td><%= link_to 'Destroy', peer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Peer', new_peer_path %>
