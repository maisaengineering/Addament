<%= form_for(@peer) do |f| %>
  <% if @peer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peer.errors.count, "error") %> prohibited this peer from being saved:</h2>

      <ul>
      <% @peer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :req_to_peer_id %><br />
    <%= f.number_field :req_to_peer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
